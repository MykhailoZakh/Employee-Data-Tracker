class Employees {
    constructor(first_name, last_name, title, salary, manager) {
        this.First_Name = first_name,
            this.Last_Name = last_name,
            this.Position = title,
            this.Salary = salary,
            this.Manager = manager
    }
};

module.exports = Employees;